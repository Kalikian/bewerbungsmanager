PGUSER=your_pg_user
PGPASSWORD=your_pg_password
PGDATABASE=bewerbungsmanager
PGHOST=localhost
PGPORT=5432

# Either use the individual PG* vars above or this DATABASE_URL:
DATABASE_URL=postgres://your_pg_user:your_pg_password@localhost:5432/bewerbungsmanager

# Auth secret (JWT/signing)
SECRET_KEY=change-me

# File storage directory
FILES_DIR=./uploads
